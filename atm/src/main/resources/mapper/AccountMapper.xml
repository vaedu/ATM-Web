<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.atm.mapper.AccountMapper">

    <select id="findByCard" resultType="com.example.atm.entity.Account">
        SELECT card, name, password, balance
        FROM account
        WHERE card = #{card}
    </select>

    <update id="updateBalance">
        UPDATE account
        SET balance = #{balance}
        WHERE card = #{card}
    </update>

    <update id="updatePassword">
        UPDATE account
        SET password = #{password}
        WHERE card = #{card}
    </update>

    <insert id="insert" parameterType="com.example.atm.entity.Account">
        INSERT INTO account (card, name, password, balance)
        VALUES (#{card}, #{name}, #{password}, #{balance})
    </insert>

</mapper>
