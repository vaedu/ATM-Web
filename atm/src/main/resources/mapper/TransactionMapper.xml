<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.atm.mapper.TransactionMapper">

    <insert id="insert" parameterType="com.example.atm.entity.Transaction">
        INSERT INTO transaction (card, type, amount, remark, time)
        VALUES (#{card}, #{type}, #{amount}, #{remark}, #{time})
    </insert>

    <select id="findByCard" resultType="com.example.atm.entity.Transaction" parameterType="String">
        SELECT id, card, type, amount, remark, time
        FROM transaction
        WHERE card = #{card}
        ORDER BY time DESC
    </select>

</mapper>
